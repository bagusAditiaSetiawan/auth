beforeAll(async () => {
    process.env.JWT_KEY = 'asdf';  
    process.env.NODE_TLS_REJECT_UNAUTHORIZED = '0';
});
  
beforeEach(async () => {
})

afterEach(async () => {
})
  
afterAll(async () => {
})